name: Request Admin Password Setup

on:
  push:
    paths:
      - ".github/workflows/request-admin-password-setup.yml"

jobs:
  recover:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Request Supabase recovery email
        run: |
          set -euo pipefail
          curl --fail-with-body --silent --show-error \
            -X POST \
            "https://sgoxywyhaketjmdmkzhm.supabase.co/auth/v1/recover" \
            -H "apikey: sb_publishable_CxLW1DX-RFPSAr8T1iSbtQ_XTIye7Yy" \
            -H "Content-Type: application/json" \
            -d '{"email":"daniel.f.guerra@gmail.com"}'
